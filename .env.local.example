MONGODB_URI=

# Server-only — do NOT prefix with NEXT_PUBLIC_. Gates /analytics and
# /api/analytics via HTTP Basic Auth (see src/middleware.ts). Leave unset
# locally; always set it in the deployed environment or the routes are open.
ANALYTICS_PASSWORD=
